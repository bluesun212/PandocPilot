\renewenvironment{align}{\[\noexpand{\begin{align}}}{\noexpand{\end{align}}\]}
\renewenvironment{align*}{\[\noexpand{\begin{align*}}}{\noexpand{\end{align*}}\]}
\renewenvironment{equation}{\[\noexpand{\begin{equation}}}{\noexpand{\end{equation}}\]}
\renewenvironment{equation*}{\[\noexpand{\begin{equation*}}}{\noexpand{\end{equation*}}\]}